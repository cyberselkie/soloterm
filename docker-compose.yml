services:
  soloterm:
    build: .
    container_name: soloterm
    volumes:
      # Mount your local project directory to /app in the container
      - ./:/app
    stdin_open: true
    tty: true
    working_dir: /app
